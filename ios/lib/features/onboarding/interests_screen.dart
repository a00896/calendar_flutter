import 'package:flutter/material.dart';

class InterestesScreen extends StatelessWidget {
  const InterestesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
